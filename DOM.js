let ID = document.getElementsByClassName('box')
console.log(ID)

// document.getElementsByClassName('box').getAttribute
// document.getElementsByClassName('box').setAttribute('class','bigBox') --> first column contain the name of attribute and second is the what we want 

// ID.style --> by this we can use style element
// Id.style.backgroundColor ='green'

// ID.textContent --> it will give the text of tag(give all the  text)
// ID.innerHTML --> it will also give the text content
// ID.innerText --> it will also give the text (all three are differnt)(something is hoddem is not seen)
// ID



