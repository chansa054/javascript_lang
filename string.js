const name="chansa"
const repo=4

// console.log(name + repo +" bavji");// we can do like this 
// console.log(`hello my name is ${name} and my repo is ${repo} and sir name is bavji`);// we use bactics(``)
const obj = new String("chansa")
// console.log(obj)// its a object we cna use my methods on it to see the methods run the code in console of browser

// console.log(obj[0])//this is not a array it is a object

// console.log(obj.charAt(2))// character at this position
// console.log(obj.indexOf('c'))// index of character
// console.log(obj.length)// length of string object
const newObj=obj.replace('chan','bham')// first one to be replace and what should be in place
