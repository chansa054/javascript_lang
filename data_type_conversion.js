let score="33abc"
console.log(typeof(score))

let valueInNumber=Number(score)
console.log(valueInNumber)

// score is not a pure number so it is returning the NaN(not a number may be) if use type of then it show number which is not write